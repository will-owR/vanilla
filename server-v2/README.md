# server-v2 (exercise_1 demo)

Run the tiny Express server and open the client at `/`:

```bash
cd server-v2
npm install
npm start
```

The server exposes `POST /api/generate` which returns a preview payload using a random file from `data/content/`.
